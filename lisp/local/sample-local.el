;;; Sample file for a `local.el' you might write

;;; local.el --- Local machine configuration

(message "Loading local config")

;; Paradox github integration
(setq paradox-github-token "some-token")

(provide 'local)
;;; local.el ends here
